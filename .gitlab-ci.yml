image: node:16-alpine

stages:
  - update-production

deploy:
  stage: update-production
  only:
    - main
  script:
    - sudo npm i -g @liara/cli@3
    - sudo liara deploy --app ticket-front --api-token $TOKEN --detach